include<stdio.h>
struct fraction
{
        int numerator;
        int denominator;
};
int elememts()
{
        int n;
        printf("enter the number of fraction to be added:");
        scanf("%d",&n);
}
void input_fraction(int n,struct a[n],struct b[n])
{
        int i;
        printf("enter the numerators of fractions to be added:");
        for(i=0;i<n;i++)
        {
                scanf("%d",&a[i]);
        }
        printf("enter the denominators of the fractions to be added:");
        for(i=0;i<n;i++)
        {
                scanf("%d",&b[i]);
        }
}
struct fraction compute_fraction(int n,struct fraction b[n])
{
        int i,struct fraction s=0;
        for(i=0;i<n;i++)
        {
                s.numerator=s.numerator*b[i]+s.denominator*a[i];
                s.denominator=s.denominator*b[i];
        }
}
void fraction(struct fraction s)
{
        printf("sum of the fractions is: %d/%d",s.numerator/s.denominator);
}
int main()
{
        int n;
        int elements();
        struct a[n],b[n],s;
        input_fraction(n,a,b);
        compute(n,a,b);
        output(s);
}
    
